/**
 * Canonical site origin — used for metadataBase, sitemap and robots.
 * The project intentionally lives on the Vercel URL (no custom domain).
 */
export const siteUrl = "https://hike-scotland-claude.vercel.app";
